import sys

print(sys.version)

print(len(sys.argv))
print(sys.argv)

# command line parameter :
# it is poossible to pass arguments to python porgrams when they are executed
# the brackets which follow main are used for this purpose
#
#
#     .argv refers to the number of arguments passed  and argv[] is a pointer array which points to each argumnets
#     passed __main__
#     python sys module
#
#  # first name of argument can be file itself
